services:
  - type: web
    name: eleganttodos
    runtime: node
    buildCommand: bash ./deploy.sh
    startCommand: node deploy-server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false