# This workflow file is deprecated and will be removed when you manually delete the .github folder
# The project now uses Render.com for deployment instead of GitHub Pages

name: Deprecated - Do Not Use

on:
  workflow_dispatch:  # Only manual triggers, not on push

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Display Info
        run: echo "This workflow is deprecated. The project now uses Render.com for deployment."